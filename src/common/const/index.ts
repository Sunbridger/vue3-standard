/**
 * @file 常量类文件, 记录cookie，localstorage, 环境变量等key的值
 */
export default class Const {
    /**
     * cookie 的key
     */
    // 登陆之后，个人信息存放标识
    static TOKEN = '_security_token';
/**
   * localStorage
   */
}
